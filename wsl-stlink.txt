In Powershell (Windows)

winget install usbipd

Once Installed, open new Powershell and run as admin

Find the BUSID
usbipd list

BIND the device (Runn in ADMIN MODE)
usbipd bind --busid <BUSID>

To Attach Run
usbipd attach --wsl --busid <BUSID>

TO Detach Run
usbipd detach --busid <BUSID>
